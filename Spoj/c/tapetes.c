#include <stdio.h>

int main(){

    int quantidade,i;

    scanf("%d",&quantidade);

    int numeros[quantidade],comparacao[quantidade];

    for (i=0;i<quantidade;i++){
        scanf("%d",&numeros[i]);
        numeros[i]=comparacao[i];
    }

    for ()

    return 0;
}
