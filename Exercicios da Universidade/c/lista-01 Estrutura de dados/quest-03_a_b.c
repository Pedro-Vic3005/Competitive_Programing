#include <stdio.h>

int main(){

    char key,choice;
    int num,count;
    long date;
    float yield;
    double price;

    // (a)
    printf("%d\n%d\n%d\n%d\n%d\n%d\n%d\n\n",&key,&choice,&num,&count,&date,&yield,&price);

    // (b)
    printf("%d\n%d\n%d\n%d\n%d\n%d\n%d\n",sizeof key,sizeof choice,sizeof num,sizeof count,sizeof date,sizeof yield,sizeof price);

    int i=10,j=20;

    void troca ()
}

